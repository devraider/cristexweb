---
- name: Reject externally supplied runtime seam internals
  ansible.builtin.assert:
    that:
      - item not in vars
    fail_msg: "INTERNAL_VARIABLE_GUARD: refusing externally supplied runtime seam internal variable {{ item }}."
  loop:
    - cristexhub_prod_runtime_manifests
    - cristexhub_prod_runtime_bootstrap_internal_preflight_binding
    - cristexhub_prod_runtime_namespace
    - cristexhub_prod_runtime_credential
    - cristexhub_prod_runtime_manifest_states
    - cristexhub_prod_runtime_operator_prerequisites
    - cristexhub_prod_runtime_generic_admission_policies
    - cristexhub_prod_runtime_generic_admission_bindings
    - cristexhub_prod_runtime_bootstrap_attestation
    - cristexhub_prod_runtime_cr_inventory
    - cristexhub_prod_runtime_target_secret_inventory
    - cristexhub_prod_runtime_cr_inventory_recheck
    - cristexhub_prod_runtime_target_secret_inventory_recheck
    - cristexhub_prod_runtime_nonadmission_prestate_recheck
    - cristexhub_prod_runtime_source_prestate_recheck
    - cristexhub_prod_runtime_secret_post_rbac_recheck
    - cristexhub_prod_runtime_operator_prerequisites_recheck
    - cristexhub_prod_runtime_credential_before_auth
    - cristexhub_prod_runtime_credential_before_static_secret
    - cristexhub_prod_runtime_source_auth_preapply_recheck
    - cristexhub_prod_runtime_source_static_secret_preapply_recheck
    - cristexhub_prod_runtime_admission_policy_recheck
    - cristexhub_prod_runtime_admission_binding_recheck
    - cristexhub_prod_runtime_expected_operator_deployment
    - cristexhub_prod_runtime_expected_operator_role
    - cristexhub_prod_runtime_expected_operator_rolebinding
    - cristexhub_prod_runtime_expected_generic_policies
    - cristexhub_prod_runtime_expected_generic_bindings
    - cristexhub_prod_runtime_admission_policies
    - cristexhub_prod_runtime_admission_bindings
    - cristexhub_prod_runtime_rbac_objects
    - cristexhub_prod_runtime_source_objects
    - cristexhub_prod_runtime_identity_keys
    - cristexhub_prod_runtime_prestate
    - cristexhub_prod_runtime_poststate
    - cristexhub_prod_runtime_admission_apply
    - cristexhub_prod_runtime_admission_policy_state
    - cristexhub_prod_runtime_admission_binding_apply
    - cristexhub_prod_runtime_admission_binding_state
    - cristexhub_prod_runtime_rbac_apply
    - cristexhub_prod_runtime_source_connection_apply
    - cristexhub_prod_runtime_source_auth_apply
    - cristexhub_prod_runtime_source_static_secret_apply
    - cristexhub_prod_runtime_source_state
    - cristexhub_prod_runtime_target_secret_poststate
    - cristexhub_prod_runtime_apply
  changed_when: false

- name: Require the guarded source-only CristexHub PROD runtime contract
  ansible.builtin.assert:
    that:
      - cristexhub_prod_runtime_bootstrap_approved | bool
      - cristexhub_prod_runtime_bootstrap_state == 'present'
      - ansible_diff_mode
      - (ansible_limit | default('')) == 'crtxweb'
      - ansible_play_hosts_all | length == 1
      - lookup('ansible.builtin.env', 'CRISTEXWEB_CRISTEXHUB_PROD_RUNTIME_ENTRYPOINT') == 'v1'
      - lookup('ansible.builtin.env', 'CRISTEXWEB_CRISTEXHUB_PROD_RUNTIME_TOKEN') is match('^[0-9a-f]{64}$')
      - lookup('ansible.builtin.env', 'CRISTEXWEB_CRISTEXHUB_PROD_RUNTIME_ATTESTATION_FILE') | length > 0
      - cristexhub_prod_runtime_bootstrap_repository_root == lookup('ansible.builtin.env', 'CRISTEXWEB_REPOSITORY_ROOT')
      - cristexhub_prod_runtime_bootstrap_kubeconfig == '/etc/rancher/k3s/k3s.yaml'
      - cristexhub_prod_runtime_bootstrap_manifest_paths | length == 13
      - cristexhub_prod_runtime_bootstrap_manifest_paths | unique | length == 13
      - cristexhub_prod_runtime_bootstrap_expected_hashes | length == 13
      - cristexhub_prod_runtime_bootstrap_expected_hashes | map(attribute='path') | list == cristexhub_prod_runtime_bootstrap_manifest_paths
      - cristexhub_prod_runtime_bootstrap_expected_hashes | map(attribute='path') | list | unique | length == 13
      - cristexhub_prod_runtime_bootstrap_namespace == 'cristexhub-prod'
      - cristexhub_prod_runtime_bootstrap_project_id == '619656da-14f3-4872-857b-be103cdc5326'
      - cristexhub_prod_runtime_bootstrap_environment_slug == 'prod'
      - cristexhub_prod_runtime_bootstrap_secret_path == '/cristexhub/prod/runtime'
      - cristexhub_prod_runtime_bootstrap_credential_name == 'cristexhub-prod-infisical-universal-auth'
      - cristexhub_prod_runtime_bootstrap_target_keys ==
        ['MONGODB_URL', 'RABBITMQ_URL', 'REDIS_URL', 'REDIS_PASSWORD', 'FERNET_KEY',
         'OIDC_CLIENT_SECRET', 'OAUTH2_PROXY_COOKIE_SECRET', 'PRIVATE_CA_BUNDLE', 'CODE_RUNNER_AUTH_TOKEN']
      - >-
        cristexhub_prod_runtime_bootstrap_labels ==
        {'app.kubernetes.io/managed-by': 'infisical', 'app.kubernetes.io/part-of': 'cristexhub',
         'cristex.io/value-owner': 'infisical-cloud'}
      - >-
        cristexhub_prod_runtime_bootstrap_credential_contract.labels ==
        {'app.kubernetes.io/managed-by': 'ansible', 'app.kubernetes.io/part-of': 'infisical-operator',
         'cristex.io/component': 'infisical-runtime-auth', 'cristex.io/value-owner': 'infisical-cloud'}
      - cristexhub_prod_runtime_bootstrap_target_contract.runtime.name == 'cristexhub-prod-runtime'
      - cristexhub_prod_runtime_bootstrap_target_contract.runtime.namespace == 'cristexhub-prod'
      - cristexhub_prod_runtime_bootstrap_target_contract.runtime.type == 'Opaque'
      - cristexhub_prod_runtime_bootstrap_target_contract.runtime['keys'] == cristexhub_prod_runtime_bootstrap_target_keys
      - cristexhub_prod_runtime_bootstrap_target_contract.ghcr_pull.name == 'cristexhub-prod-ghcr-pull'
      - cristexhub_prod_runtime_bootstrap_target_contract.ghcr_pull.namespace == 'cristexhub-prod'
      - cristexhub_prod_runtime_bootstrap_target_contract.ghcr_pull.type == 'kubernetes.io/dockerconfigjson'
      - cristexhub_prod_runtime_bootstrap_target_contract.ghcr_pull['keys'] == ['.dockerconfigjson']
      - cristexhub_prod_runtime_bootstrap_credential_contract.name == 'cristexhub-prod-infisical-universal-auth'
      - cristexhub_prod_runtime_bootstrap_credential_contract.namespace == 'cristexhub-prod'
      - cristexhub_prod_runtime_bootstrap_credential_contract.type == 'Opaque'
      - cristexhub_prod_runtime_bootstrap_credential_contract['keys'] == ['clientId', 'clientSecret']
      - >-
        cristexhub_prod_runtime_bootstrap_expected_cr_kinds ==
        [
          {'kind': 'InfisicalSecret', 'api_version': 'secrets.infisical.com/v1alpha1'},
          {'kind': 'InfisicalPushSecret', 'api_version': 'secrets.infisical.com/v1alpha1'},
          {'kind': 'InfisicalDynamicSecret', 'api_version': 'secrets.infisical.com/v1alpha1'},
          {'kind': 'InfisicalConnection', 'api_version': 'secrets.infisical.com/v1beta1'},
          {'kind': 'InfisicalAuth', 'api_version': 'secrets.infisical.com/v1beta1'},
          {'kind': 'InfisicalStaticSecret', 'api_version': 'secrets.infisical.com/v1beta1'}
        ]
      - >-
        cristexhub_prod_runtime_bootstrap_expected_target_secret_names ==
        ['cristexhub-prod-infisical-universal-auth', 'cristexhub-prod-runtime', 'cristexhub-prod-ghcr-pull']
    fail_msg: Refusing an unapproved, selected, non-diff, or altered runtime seam invocation.
  changed_when: false

- name: Inspect one-time runtime seam attestation
  ansible.builtin.stat:
    path: "{{ lookup('ansible.builtin.env', 'CRISTEXWEB_CRISTEXHUB_PROD_RUNTIME_ATTESTATION_FILE') }}"
    follow: false
  register: cristexhub_prod_runtime_bootstrap_attestation
  delegate_to: localhost
  changed_when: false
  no_log: true

- name: Require private runtime seam attestation
  ansible.builtin.assert:
    that:
      - cristexhub_prod_runtime_bootstrap_attestation.stat.isreg | default(false)
      - not (cristexhub_prod_runtime_bootstrap_attestation.stat.islnk | default(false))
      - cristexhub_prod_runtime_bootstrap_attestation.stat.mode == '0600'
      - lookup('ansible.builtin.file', lookup('ansible.builtin.env', 'CRISTEXWEB_CRISTEXHUB_PROD_RUNTIME_ATTESTATION_FILE')) == lookup('ansible.builtin.env', 'CRISTEXWEB_CRISTEXHUB_PROD_RUNTIME_TOKEN') ~ ':entrypoint'
    fail_msg: Refusing missing or invalid runtime seam attestation.
  changed_when: false
  no_log: true

- name: Query required runtime namespace
  kubernetes.core.k8s_info:
    api_version: v1
    kind: Namespace
    name: cristexhub-prod
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  register: cristexhub_prod_runtime_namespace
  changed_when: false

- name: Require precreated target namespace
  ansible.builtin.assert:
    that:
      - cristexhub_prod_runtime_namespace.resources | length == 1
      - cristexhub_prod_runtime_namespace.resources[0].status.phase == 'Active'
      - cristexhub_prod_runtime_namespace.resources[0].metadata.labels.get('app.kubernetes.io/part-of') == 'cristexhub'
      - cristexhub_prod_runtime_namespace.resources[0].metadata.labels.get('cristex.io/environment') == 'prod'
      - cristexhub_prod_runtime_namespace.resources[0].metadata.labels.get('cristex.io/bootstrap-writer') == 'ansible'
      - cristexhub_prod_runtime_namespace.resources[0].metadata.labels.get('cristex.io/desired-owner') == 'argocd'
    fail_msg: Refusing to create or reconcile a missing or drifted application namespace.
  changed_when: false

- name: Query separately approved Infisical Operator PROD prerequisites
  kubernetes.core.k8s_info:
    api_version: "{{ item.api_version }}"
    kind: "{{ item.kind }}"
    namespace: "{{ item.namespace }}"
    name: "{{ item.name }}"
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  loop:
    - api_version: apps/v1
      kind: Deployment
      namespace: shared-services
      name: infisical-operator-controller
      register_name: deployment
    - api_version: rbac.authorization.k8s.io/v1
      kind: Role
      namespace: cristexhub-prod
      name: infisical-operator-manager
      register_name: role
    - api_version: rbac.authorization.k8s.io/v1
      kind: RoleBinding
      namespace: cristexhub-prod
      name: infisical-operator-manager
      register_name: rolebinding
  register: cristexhub_prod_runtime_operator_prerequisites
  changed_when: false
  no_log: true

- name: Query generic Infisical admission prerequisites
  kubernetes.core.k8s_info:
    api_version: admissionregistration.k8s.io/v1
    kind: ValidatingAdmissionPolicy
    name: "{{ item }}"
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  loop:
    - infisical-auth-boundary
    - infisical-connection-boundary
    - infisical-static-secret-boundary
  register: cristexhub_prod_runtime_generic_admission_policies
  changed_when: false
  no_log: true

- name: Query generic Infisical admission binding prerequisites
  kubernetes.core.k8s_info:
    api_version: admissionregistration.k8s.io/v1
    kind: ValidatingAdmissionPolicyBinding
    name: "{{ item }}"
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  loop:
    - infisical-auth-boundary
    - infisical-connection-boundary
    - infisical-static-secret-boundary
  register: cristexhub_prod_runtime_generic_admission_bindings
  changed_when: false
  no_log: true

- name: Load exact Operator and generic admission source contracts
  ansible.builtin.set_fact:
    cristexhub_prod_runtime_expected_operator_deployment: >-
      {{ lookup('ansible.builtin.file', cristexhub_prod_runtime_bootstrap_repository_root ~
         '/ansible/files/components/infisical-operator/controller/deployment.yaml') | from_yaml }}
    cristexhub_prod_runtime_expected_operator_role: >-
      {{ lookup('ansible.builtin.file', cristexhub_prod_runtime_bootstrap_repository_root ~
         '/ansible/files/components/infisical-operator/rbac/manager-role-cristexhub-prod.yaml') | from_yaml }}
    cristexhub_prod_runtime_expected_operator_rolebinding: >-
      {{ lookup('ansible.builtin.file', cristexhub_prod_runtime_bootstrap_repository_root ~
         '/ansible/files/components/infisical-operator/rbac/manager-rolebinding-cristexhub-prod.yaml') | from_yaml }}
    cristexhub_prod_runtime_expected_generic_policies:
      - "{{ lookup('ansible.builtin.file', cristexhub_prod_runtime_bootstrap_repository_root ~ '/ansible/files/components/infisical-operator/admission/infisical-auth-boundary.yaml') | from_yaml }}"
      - "{{ lookup('ansible.builtin.file', cristexhub_prod_runtime_bootstrap_repository_root ~ '/ansible/files/components/infisical-operator/admission/infisical-connection-boundary.yaml') | from_yaml }}"
      - "{{ lookup('ansible.builtin.file', cristexhub_prod_runtime_bootstrap_repository_root ~ '/ansible/files/components/infisical-operator/admission/infisical-static-secret-boundary.yaml') | from_yaml }}"
    cristexhub_prod_runtime_expected_generic_bindings:
      - "{{ lookup('ansible.builtin.file', cristexhub_prod_runtime_bootstrap_repository_root ~ '/ansible/files/components/infisical-operator/admission/infisical-auth-boundary-binding.yaml') | from_yaml }}"
      - "{{ lookup('ansible.builtin.file', cristexhub_prod_runtime_bootstrap_repository_root ~ '/ansible/files/components/infisical-operator/admission/infisical-connection-boundary-binding.yaml') | from_yaml }}"
      - "{{ lookup('ansible.builtin.file', cristexhub_prod_runtime_bootstrap_repository_root ~ '/ansible/files/components/infisical-operator/admission/infisical-static-secret-boundary-binding.yaml') | from_yaml }}"
  changed_when: false
  no_log: true

- name: Query every Infisical custom-resource kind in PROD
  kubernetes.core.k8s_info:
    api_version: "{{ item.api_version }}"
    kind: "{{ item.kind }}"
    namespace: cristexhub-prod
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  loop: "{{ cristexhub_prod_runtime_bootstrap_expected_cr_kinds }}"
  register: cristexhub_prod_runtime_cr_inventory
  changed_when: false
  no_log: true

- name: Query every Secret in the PROD Namespace before mutation
  kubernetes.core.k8s_info:
    api_version: v1
    kind: Secret
    namespace: cristexhub-prod
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  register: cristexhub_prod_runtime_target_secret_inventory
  changed_when: false
  no_log: true

- name: Reject noncanonical existing PROD Infisical resources and Secrets
  ansible.builtin.assert:
    that:
      - cristexhub_prod_runtime_cr_inventory.results | length == 6
      - >-
        item.item.kind in ['InfisicalSecret', 'InfisicalPushSecret', 'InfisicalDynamicSecret']
        and item.resources | length == 0 or
        item.item.kind == 'InfisicalConnection' and
        (item.resources | map(attribute='metadata.name') | list | difference(['infisical-cloud']) | length == 0) or
        item.item.kind == 'InfisicalAuth' and
        (item.resources | map(attribute='metadata.name') | list | difference(['cristexhub-prod-infisical-auth']) | length == 0) or
        item.item.kind == 'InfisicalStaticSecret' and
        (item.resources | map(attribute='metadata.name') | list | difference(['cristexhub-prod-runtime']) | length == 0)
      - item.resources | length <= 1
    fail_msg: Refusing an unexpected or duplicate PROD Infisical custom resource.
  loop: "{{ cristexhub_prod_runtime_cr_inventory.results }}"
  loop_control:
    label: "{{ item.item.kind }}"
  changed_when: false
  no_log: true

- name: Reject noncanonical existing PROD target Secret names
  ansible.builtin.assert:
    that:
      - item.resources | map(attribute='metadata.name') | list | difference(cristexhub_prod_runtime_bootstrap_expected_target_secret_names) | length == 0
      - item.resources | map(attribute='metadata.name') | list | unique | length == item.resources | length
    fail_msg: Refusing an unexpected or duplicate PROD Secret before runtime seam mutation.
  loop:
    - "{{ cristexhub_prod_runtime_target_secret_inventory }}"
  loop_control:
    label: cristexhub-prod
  changed_when: false
  no_log: true

- name: Reject drifted existing PROD target Secret contracts
  ansible.builtin.assert:
    that:
      - >-
        cristexhub_prod_runtime_target_secret_inventory.resources |
        selectattr('metadata.name', 'equalto', item.value.name) | list | length in [0, 1]
      - >-
        cristexhub_prod_runtime_target_secret_inventory.resources |
        selectattr('metadata.name', 'equalto', item.value.name) | list | length == 0 or
        (cristexhub_prod_runtime_target_secret_inventory.resources |
         selectattr('metadata.name', 'equalto', item.value.name) | first).type == item.value.type
      - >-
        cristexhub_prod_runtime_target_secret_inventory.resources |
        selectattr('metadata.name', 'equalto', item.value.name) | list | length == 0 or
        ((cristexhub_prod_runtime_target_secret_inventory.resources |
          selectattr('metadata.name', 'equalto', item.value.name) | first).data | default({})).keys() | list | sort == item.value['keys'] | sort
      - >-
        cristexhub_prod_runtime_target_secret_inventory.resources |
        selectattr('metadata.name', 'equalto', item.value.name) | list | length == 0 or
        (cristexhub_prod_runtime_target_secret_inventory.resources |
         selectattr('metadata.name', 'equalto', item.value.name) | first).binaryData | default({}) | length == 0
      - >-
        cristexhub_prod_runtime_target_secret_inventory.resources |
        selectattr('metadata.name', 'equalto', item.value.name) | list | length == 0 or
        not ((cristexhub_prod_runtime_target_secret_inventory.resources |
         selectattr('metadata.name', 'equalto', item.value.name) | first).immutable | default(false) | bool)
      - >-
        cristexhub_prod_runtime_target_secret_inventory.resources |
        selectattr('metadata.name', 'equalto', item.value.name) | list | length == 0 or
        (cristexhub_prod_runtime_target_secret_inventory.resources |
         selectattr('metadata.name', 'equalto', item.value.name) | first).metadata.ownerReferences | default([]) | length == 0
      - >-
        cristexhub_prod_runtime_target_secret_inventory.resources |
        selectattr('metadata.name', 'equalto', item.value.name) | list | length == 0 or
        (cristexhub_prod_runtime_target_secret_inventory.resources |
         selectattr('metadata.name', 'equalto', item.value.name) | first).metadata.finalizers | default([]) | length == 0
      - >-
        cristexhub_prod_runtime_target_secret_inventory.resources |
        selectattr('metadata.name', 'equalto', item.value.name) | list | length == 0 or
        ((cristexhub_prod_runtime_target_secret_inventory.resources |
          selectattr('metadata.name', 'equalto', item.value.name) | first).metadata.labels | default({})) == cristexhub_prod_runtime_bootstrap_labels
      - >-
        cristexhub_prod_runtime_target_secret_inventory.resources |
        selectattr('metadata.name', 'equalto', item.value.name) | list | length == 0 or
        ((cristexhub_prod_runtime_target_secret_inventory.resources |
          selectattr('metadata.name', 'equalto', item.value.name) | first).metadata.annotations | default({})).keys() | list == ['secrets.infisical.com/version']
      - >-
        cristexhub_prod_runtime_target_secret_inventory.resources |
        selectattr('metadata.name', 'equalto', item.value.name) | list | length == 0 or
        (cristexhub_prod_runtime_target_secret_inventory.resources |
         selectattr('metadata.name', 'equalto', item.value.name) | first).metadata.annotations['secrets.infisical.com/version'] | length > 0
    fail_msg: Refusing an existing drifted PROD runtime target Secret.
  loop: "{{ cristexhub_prod_runtime_bootstrap_target_contract | dict2items }}"
  loop_control:
    label: "{{ item.value.name }}"
  changed_when: false
  no_log: true

- name: Stop until the Infisical Operator PROD checkpoint is separately approved
  ansible.builtin.assert:
    that:
      - cristexhub_prod_runtime_operator_prerequisites.results | length == 3
      - cristexhub_prod_runtime_operator_prerequisites.results | map(attribute='resources') | map('length') | list == [1, 1, 1]
      - >-
        cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].status.observedGeneration ==
        cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].metadata.generation
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].status.replicas | default(0) | int == 1
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].status.updatedReplicas | default(0) | int == 1
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].status.readyReplicas | default(0) | int == 1
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].status.availableReplicas | default(0) | int == 1
      - >-
        cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].status.conditions |
        selectattr('type', 'equalto', 'Available') | selectattr('status', 'equalto', 'True') |
        list | length == 1
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.replicas == 1
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.strategy == cristexhub_prod_runtime_expected_operator_deployment.spec.strategy
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.selector == cristexhub_prod_runtime_expected_operator_deployment.spec.selector
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.metadata.labels == cristexhub_prod_runtime_expected_operator_deployment.spec.template.metadata.labels
      - >-
        cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.metadata.annotations.keys() |
        list | difference(cristexhub_prod_runtime_expected_operator_deployment.spec.template.metadata.annotations.keys() |
        list + ['kubectl.kubernetes.io/restartedAt']) | length == 0
      - >-
        cristexhub_prod_runtime_expected_operator_deployment.spec.template.metadata.annotations |
        dict2items | rejectattr('value', 'equalto', none) | list | length == 2
      - >-
        cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.metadata.annotations['cristex.io/identity-access-generation'] ==
        cristexhub_prod_runtime_expected_operator_deployment.spec.template.metadata.annotations['cristex.io/identity-access-generation']
      - >-
        cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.metadata.annotations['cristex.io/proxy-config-sha256'] ==
        cristexhub_prod_runtime_expected_operator_deployment.spec.template.metadata.annotations['cristex.io/proxy-config-sha256']
      - >-
        'kubectl.kubernetes.io/restartedAt' not in
        cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.metadata.annotations or
        cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.metadata.annotations['kubectl.kubernetes.io/restartedAt']
        is match('^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(Z|[+-][0-9]{2}:[0-9]{2})$')
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.serviceAccountName == 'infisical-operator-controller'
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.automountServiceAccountToken | bool
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers | length == 1
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].name == 'manager'
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].image == cristexhub_prod_runtime_expected_operator_deployment.spec.template.spec.containers[0].image
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].args == cristexhub_prod_runtime_expected_operator_deployment.spec.template.spec.containers[0].args
      - >-
        cristexhub_prod_runtime_expected_operator_deployment.spec.template.spec.containers[0].args ==
        ['--metrics-bind-address=0', '--leader-elect', '--health-probe-bind-address=:8081',
         '--namespaces=shared-services,argocd,cristexhub-dev,cristexhub-prod,platform-edge']
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.revisionHistoryLimit == cristexhub_prod_runtime_expected_operator_deployment.spec.revisionHistoryLimit
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.securityContext == cristexhub_prod_runtime_expected_operator_deployment.spec.template.spec.securityContext
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.volumes == cristexhub_prod_runtime_expected_operator_deployment.spec.template.spec.volumes
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.initContainers | default([]) | length == 0
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.ephemeralContainers | default([]) | length == 0
      - not (cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.hostNetwork | default(false) | bool)
      - not (cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.hostPID | default(false) | bool)
      - not (cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.hostIPC | default(false) | bool)
      - not (cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.shareProcessNamespace | default(false) | bool)
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.imagePullSecrets | default([]) | length == 0
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.terminationGracePeriodSeconds == cristexhub_prod_runtime_expected_operator_deployment.spec.template.spec.terminationGracePeriodSeconds
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].command == cristexhub_prod_runtime_expected_operator_deployment.spec.template.spec.containers[0].command
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].imagePullPolicy == cristexhub_prod_runtime_expected_operator_deployment.spec.template.spec.containers[0].imagePullPolicy
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].env == cristexhub_prod_runtime_expected_operator_deployment.spec.template.spec.containers[0].env
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].ports == cristexhub_prod_runtime_expected_operator_deployment.spec.template.spec.containers[0].ports
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].securityContext == cristexhub_prod_runtime_expected_operator_deployment.spec.template.spec.containers[0].securityContext
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].resources == cristexhub_prod_runtime_expected_operator_deployment.spec.template.spec.containers[0].resources
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].livenessProbe.httpGet.path == '/healthz'
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].livenessProbe.httpGet.port == 'health'
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].livenessProbe.initialDelaySeconds == 15
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].livenessProbe.periodSeconds == 20
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].livenessProbe.timeoutSeconds == 1
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].livenessProbe.successThreshold == 1
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].livenessProbe.failureThreshold == 3
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].readinessProbe.httpGet.path == '/readyz'
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].readinessProbe.httpGet.port == 'health'
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].readinessProbe.initialDelaySeconds == 5
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].readinessProbe.periodSeconds == 10
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].readinessProbe.timeoutSeconds == 1
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].readinessProbe.successThreshold == 1
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].readinessProbe.failureThreshold == 3
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].volumeMounts == cristexhub_prod_runtime_expected_operator_deployment.spec.template.spec.containers[0].volumeMounts
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].envFrom | default([]) | length == 0
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].lifecycle | default({}, true) | length == 0
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].spec.template.spec.containers[0].startupProbe | default({}, true) | length == 0
      - cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].metadata.labels == cristexhub_prod_runtime_expected_operator_deployment.metadata.labels
      - cristexhub_prod_runtime_operator_prerequisites.results[1].resources[0].rules == cristexhub_prod_runtime_expected_operator_role.rules
      - cristexhub_prod_runtime_operator_prerequisites.results[1].resources[0].metadata.labels == cristexhub_prod_runtime_expected_operator_role.metadata.labels
      - cristexhub_prod_runtime_operator_prerequisites.results[2].resources[0].roleRef == cristexhub_prod_runtime_expected_operator_rolebinding.roleRef
      - cristexhub_prod_runtime_operator_prerequisites.results[2].resources[0].subjects == cristexhub_prod_runtime_expected_operator_rolebinding.subjects
      - cristexhub_prod_runtime_operator_prerequisites.results[2].resources[0].metadata.labels == cristexhub_prod_runtime_expected_operator_rolebinding.metadata.labels
      - cristexhub_prod_runtime_generic_admission_policies.results | map(attribute='resources') | map('length') | list == [1, 1, 1]
      - cristexhub_prod_runtime_generic_admission_policies.results | map(attribute='resources.0.metadata.name') | list == ['infisical-auth-boundary', 'infisical-connection-boundary', 'infisical-static-secret-boundary']
      - cristexhub_prod_runtime_generic_admission_policies.results | map(attribute='resources.0.spec.matchConstraints.resourceRules') | list == cristexhub_prod_runtime_expected_generic_policies | map(attribute='spec.matchConstraints.resourceRules') | list
      - cristexhub_prod_runtime_generic_admission_policies.results | map(attribute='resources.0.spec.validations') | list == cristexhub_prod_runtime_expected_generic_policies | map(attribute='spec.validations') | list
      - cristexhub_prod_runtime_generic_admission_bindings.results | map(attribute='resources') | map('length') | list == [1, 1, 1]
      - cristexhub_prod_runtime_generic_admission_bindings.results | map(attribute='resources.0.metadata.name') | list == ['infisical-auth-boundary', 'infisical-connection-boundary', 'infisical-static-secret-boundary']
      - cristexhub_prod_runtime_generic_admission_bindings.results | map(attribute='resources.0.spec.policyName') | list == ['infisical-auth-boundary', 'infisical-connection-boundary', 'infisical-static-secret-boundary']
      - >-
        cristexhub_prod_runtime_generic_admission_policies.results |
        map(attribute='resources.0.spec.failurePolicy') | list | unique | list == ['Fail']
      - >-
        cristexhub_prod_runtime_generic_admission_bindings.results |
        map(attribute='resources.0.spec.validationActions') | list | unique | list == [['Deny']]
      - >-
        cristexhub_prod_runtime_generic_admission_policies.results |
        map(attribute='resources.0.spec.validations') | map('to_json') |
        select('search', 'cristexhub-prod') | list | length == 3
    fail_msg: "BLOCKED: the existing Infisical Operator closure does not yet have separately approved cristexhub-prod watch/RBAC/admission. No runtime seam mutation was attempted."
  changed_when: false
  no_log: true

- name: Require generic PROD admission policies to be type-checked at observed generation
  ansible.builtin.assert:
    that:
      - item.resources | length == 1
      - item.resources[0].status.observedGeneration == item.resources[0].metadata.generation
      - item.resources[0].status.typeChecking is defined
      - item.resources[0].status.typeChecking.expressionWarnings | default([]) | length == 0
      - >-
        item.resources[0].status.conditions | default([]) | length == 0 or
        (item.resources[0].status.conditions | default([]) |
         selectattr('type', 'equalto', 'TypeChecking') |
         selectattr('status', 'equalto', 'True') |
         selectattr('observedGeneration', 'equalto', item.resources[0].metadata.generation) |
         list | length == 1)
    fail_msg: Refusing runtime seam mutation until every generic PROD admission policy is type-checked at its observed generation.
  loop: "{{ cristexhub_prod_runtime_generic_admission_policies.results }}"
  loop_control:
    label: "{{ item.item }}"
  changed_when: false
  no_log: true

- name: Inspect canonical runtime seam manifests
  ansible.builtin.stat:
    path: "{{ item }}"
    follow: false
    get_checksum: true
    checksum_algorithm: sha256
  loop: "{{ cristexhub_prod_runtime_bootstrap_manifest_paths }}"
  register: cristexhub_prod_runtime_manifest_states
  delegate_to: localhost
  changed_when: false

- name: Require regular canonical runtime seam manifests
  ansible.builtin.assert:
    that:
      - item.stat.isreg | default(false)
      - not (item.stat.islnk | default(false))
      - item.stat.mode == '0644'
      - item.stat.checksum == (cristexhub_prod_runtime_bootstrap_expected_hashes | selectattr('path', 'equalto', item.item) | map(attribute='sha256') | first)
      - (item.item | ansible.builtin.realpath) == item.item
      - item.item.startswith(cristexhub_prod_runtime_bootstrap_repository_root ~ '/ansible/files/components/infisical-cristexhub-prod-runtime/')
    fail_msg: Refusing missing, symlinked, mode-drifted, or hash-drifted runtime seam source.
  loop: "{{ cristexhub_prod_runtime_manifest_states.results }}"
  changed_when: false

- name: Load exact value-free runtime seam objects
  ansible.builtin.set_fact:
    cristexhub_prod_runtime_manifests: "{{ (cristexhub_prod_runtime_manifests | default([])) + [(lookup('ansible.builtin.file', item) | from_yaml)] }}"
  loop: "{{ cristexhub_prod_runtime_bootstrap_manifest_paths }}"
  changed_when: false
  no_log: true

- name: Derive the ordered runtime seam identity closure
  ansible.builtin.set_fact:
    cristexhub_prod_runtime_identity_keys: >-
      {{ (cristexhub_prod_runtime_identity_keys | default([])) +
         [item.apiVersion ~ '|' ~ item.kind ~ '|' ~
          (item.metadata.namespace | default('')) ~ '|' ~ item.metadata.name] }}
  loop: "{{ cristexhub_prod_runtime_manifests }}"
  changed_when: false
  no_log: true

- name: Require exact 13-object value-free closure
  ansible.builtin.assert:
    that:
      - cristexhub_prod_runtime_manifests | length == 13
      - cristexhub_prod_runtime_identity_keys | length == 13
      - cristexhub_prod_runtime_identity_keys | unique | length == 13
      - >-
        cristexhub_prod_runtime_identity_keys | sort | join('\n') | hash('sha256') ==
        'd46dde754f05f248bbc19ff28a65ec44ccb1f84c53221c1f7d4e3bb355d6ff20'
      - cristexhub_prod_runtime_manifests | selectattr('kind', 'equalto', 'Secret') | list | length == 0
      - cristexhub_prod_runtime_manifests | selectattr('metadata.namespace', 'defined') | map(attribute='metadata.namespace') | unique | difference(['cristexhub-prod']) | length == 0
      - cristexhub_prod_runtime_manifests | selectattr('kind', 'equalto', 'ValidatingAdmissionPolicy') | list | length == 4
      - cristexhub_prod_runtime_manifests | selectattr('kind', 'equalto', 'ValidatingAdmissionPolicyBinding') | list | length == 4
      - cristexhub_prod_runtime_manifests | selectattr('kind', 'equalto', 'Role') | list | length == 1
      - cristexhub_prod_runtime_manifests | selectattr('kind', 'equalto', 'RoleBinding') | list | length == 1
      - cristexhub_prod_runtime_manifests | selectattr('kind', 'equalto', 'InfisicalConnection') | list | length == 1
      - cristexhub_prod_runtime_manifests | selectattr('kind', 'equalto', 'InfisicalAuth') | list | length == 1
      - cristexhub_prod_runtime_manifests | selectattr('kind', 'equalto', 'InfisicalStaticSecret') | list | length == 1
      - cristexhub_prod_runtime_identity_keys[8:] ==
        ['rbac.authorization.k8s.io/v1|Role|cristexhub-prod|infisical-cristexhub-prod-runtime-secret-writer',
         'rbac.authorization.k8s.io/v1|RoleBinding|cristexhub-prod|infisical-cristexhub-prod-runtime-secret-writer',
         'secrets.infisical.com/v1beta1|InfisicalConnection|cristexhub-prod|infisical-cloud',
         'secrets.infisical.com/v1beta1|InfisicalAuth|cristexhub-prod|cristexhub-prod-infisical-auth',
         'secrets.infisical.com/v1beta1|InfisicalStaticSecret|cristexhub-prod|cristexhub-prod-runtime']
    fail_msg: Refusing a noncanonical runtime seam object closure.
  changed_when: false
  no_log: true

- name: Refuse absent runtime Universal Auth prerequisite before any mutation
  kubernetes.core.k8s_info:
    api_version: v1
    kind: Secret
    name: cristexhub-prod-infisical-universal-auth
    namespace: cristexhub-prod
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  register: cristexhub_prod_runtime_credential
  changed_when: false
  no_log: true

- name: Stop until runtime Universal Auth is separately materialized
  ansible.builtin.assert:
    that:
      - cristexhub_prod_runtime_credential.resources | length == 1
      - cristexhub_prod_runtime_credential.resources[0].metadata.name == cristexhub_prod_runtime_bootstrap_credential_contract.name
      - cristexhub_prod_runtime_credential.resources[0].metadata.namespace == cristexhub_prod_runtime_bootstrap_credential_contract.namespace
      - cristexhub_prod_runtime_credential.resources[0].type == cristexhub_prod_runtime_bootstrap_credential_contract.type
      - (cristexhub_prod_runtime_credential.resources[0].data | default({})).keys() | list | sort == cristexhub_prod_runtime_bootstrap_credential_contract['keys'] | sort
      - >-
        (cristexhub_prod_runtime_credential.resources[0].data | default({})) |
        dict2items | map(attribute='value') | map('length') | min > 0
      - (cristexhub_prod_runtime_credential.resources[0].binaryData | default({})).keys() | list | length == 0
      - not (cristexhub_prod_runtime_credential.resources[0].immutable | default(false) | bool)
      - (cristexhub_prod_runtime_credential.resources[0].metadata.ownerReferences | default([]) | length) == 0
      - (cristexhub_prod_runtime_credential.resources[0].metadata.finalizers | default([]) | length) == 0
      - (cristexhub_prod_runtime_credential.resources[0].metadata.labels | default({})) == cristexhub_prod_runtime_bootstrap_credential_contract.labels
      - (cristexhub_prod_runtime_credential.resources[0].metadata.annotations | default({})) | length == 0
      - cristexhub_prod_runtime_credential.resources[0].metadata.uid | default('') | length > 0
      - cristexhub_prod_runtime_credential.resources[0].metadata.resourceVersion | default('') | length > 0
    fail_msg: "BLOCKED: precreate exact cristexhub-prod-infisical-universal-auth metadata (clientId/clientSecret) before runtime seam apply. No mutation was attempted."
  changed_when: false
  no_log: true

- name: Query exact runtime seam object pre-state
  kubernetes.core.k8s_info:
    api_version: "{{ item.apiVersion }}"
    kind: "{{ item.kind }}"
    namespace: "{{ item.metadata.namespace | default(omit) }}"
    name: "{{ item.metadata.name }}"
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  loop: "{{ cristexhub_prod_runtime_manifests }}"
  loop_control:
    label: "{{ item.kind }}/{{ item.metadata.name }}"
  register: cristexhub_prod_runtime_prestate
  changed_when: false
  no_log: true

- name: Reject foreign runtime seam objects before mutation
  ansible.builtin.assert:
    that:
      - item.resources | length in [0, 1]
      - >-
        item.resources | length == 0 or
        (item.resources[0].apiVersion == item.item.apiVersion and
         item.resources[0].kind == item.item.kind and
         item.resources[0].metadata.name == item.item.metadata.name and
         (item.resources[0].metadata.namespace | default('')) == (item.item.metadata.namespace | default('')) and
         (item.resources[0].metadata.labels | default({})) == (item.item.metadata.labels | default({})) and
         (item.resources[0].metadata.annotations | default({})) == (item.item.metadata.annotations | default({})) and
         (item.resources[0].metadata.ownerReferences | default([]) | length) == 0 and
         (item.resources[0].metadata.finalizers | default([]) | length) == 0 and
         ((item.item.kind in ['InfisicalConnection', 'InfisicalAuth', 'InfisicalStaticSecret'] and
           item.resources[0].spec == item.item.spec) or
          (item.item.kind == 'Role' and item.resources[0].rules == item.item.rules) or
          (item.item.kind == 'RoleBinding' and item.resources[0].roleRef == item.item.roleRef and
           item.resources[0].subjects == item.item.subjects) or
          (item.item.kind == 'ValidatingAdmissionPolicy' and
           item.resources[0].spec.failurePolicy == item.item.spec.failurePolicy and
           item.resources[0].spec.matchConstraints.resourceRules == item.item.spec.matchConstraints.resourceRules and
           (item.resources[0].spec.matchConditions | default([])) == (item.item.spec.matchConditions | default([])) and
           item.resources[0].spec.validations == item.item.spec.validations) or
          (item.item.kind == 'ValidatingAdmissionPolicyBinding' and
           item.resources[0].spec.policyName == item.item.spec.policyName and
           item.resources[0].spec.validationActions == item.item.spec.validationActions and
           (item.resources[0].spec.matchResources | default({})) == (item.item.spec.matchResources | default({})))))
    fail_msg: Refusing silent adoption of a foreign or drifted runtime seam object.
  loop: "{{ cristexhub_prod_runtime_prestate.results }}"
  loop_control:
    label: "{{ item.item.kind }}/{{ item.item.metadata.name }}"
  changed_when: false
  no_log: true

- name: Partition the exact runtime seam into staged mutation sets
  ansible.builtin.set_fact:
    cristexhub_prod_runtime_admission_policies: "{{ cristexhub_prod_runtime_manifests | selectattr('kind', 'equalto', 'ValidatingAdmissionPolicy') | list }}"
    cristexhub_prod_runtime_admission_bindings: "{{ cristexhub_prod_runtime_manifests | selectattr('kind', 'equalto', 'ValidatingAdmissionPolicyBinding') | list }}"
    cristexhub_prod_runtime_rbac_objects: "{{ cristexhub_prod_runtime_manifests | selectattr('kind', 'in', ['Role', 'RoleBinding']) | list }}"
    cristexhub_prod_runtime_source_objects: "{{ cristexhub_prod_runtime_manifests | selectattr('kind', 'in', ['InfisicalConnection', 'InfisicalAuth', 'InfisicalStaticSecret']) | list }}"
  changed_when: false
  no_log: true

- name: Bind exact runtime seam attestation to the 13-object closure
  ansible.builtin.set_fact:
    cristexhub_prod_runtime_bootstrap_internal_preflight_binding:
      attestation_sha256: "{{ lookup('ansible.builtin.env', 'CRISTEXWEB_CRISTEXHUB_PROD_RUNTIME_TOKEN') | hash('sha256') }}"
      object_count: 13
      prestate_count: "{{ cristexhub_prod_runtime_prestate.results | length }}"
      operator_prerequisite_count: 3
      generic_policy_count: 3
      generic_binding_count: 3
      cr_inventory_count: "{{ cristexhub_prod_runtime_cr_inventory.results | length }}"
      target_secret_inventory_count: "{{ cristexhub_prod_runtime_target_secret_inventory.resources | length }}"
      identity_set_sha256: d46dde754f05f248bbc19ff28a65ec44ccb1f84c53221c1f7d4e3bb355d6ff20
      identity_keys_sha256: >-
        {{ cristexhub_prod_runtime_identity_keys | sort | join('\n') | hash('sha256') }}
  changed_when: false
  no_log: true

- name: Reconcile exact runtime seam admission policies first
  infisical_cristexhub_prod_runtime_guarded_k8s:
    state: present
    definition: "{{ item }}"
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
    wait: false
    wait_timeout: 60
  loop: "{{ cristexhub_prod_runtime_admission_policies }}"
  loop_control:
    label: "{{ item.metadata.name }}"
  register: cristexhub_prod_runtime_admission_apply
  no_log: true

- name: Wait for exact runtime VAP type-checking and observed generation
  kubernetes.core.k8s_info:
    api_version: admissionregistration.k8s.io/v1
    kind: ValidatingAdmissionPolicy
    name: "{{ item.metadata.name }}"
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  loop: "{{ cristexhub_prod_runtime_admission_policies }}"
  loop_control:
    label: "{{ item.metadata.name }}"
  register: cristexhub_prod_runtime_admission_policy_state
  until:
    - cristexhub_prod_runtime_admission_policy_state.resources | length == 1
    - cristexhub_prod_runtime_admission_policy_state.resources[0].spec.failurePolicy == item.spec.failurePolicy
    - cristexhub_prod_runtime_admission_policy_state.resources[0].spec.matchConstraints.resourceRules == item.spec.matchConstraints.resourceRules
    - (cristexhub_prod_runtime_admission_policy_state.resources[0].spec.matchConditions | default([])) == (item.spec.matchConditions | default([]))
    - cristexhub_prod_runtime_admission_policy_state.resources[0].spec.validations == item.spec.validations
    - cristexhub_prod_runtime_admission_policy_state.resources[0].status.observedGeneration == cristexhub_prod_runtime_admission_policy_state.resources[0].metadata.generation
    - cristexhub_prod_runtime_admission_policy_state.resources[0].status.typeChecking is defined
    - cristexhub_prod_runtime_admission_policy_state.resources[0].status.typeChecking.expressionWarnings | default([]) | length == 0
    - >-
      cristexhub_prod_runtime_admission_policy_state.resources[0].status.conditions | default([]) | length == 0 or
      (cristexhub_prod_runtime_admission_policy_state.resources[0].status.conditions | default([]) |
       selectattr('type', 'equalto', 'TypeChecking') |
       selectattr('status', 'equalto', 'True') |
       selectattr('observedGeneration', 'equalto', cristexhub_prod_runtime_admission_policy_state.resources[0].metadata.generation) |
       list | length == 1)
  retries: 30
  delay: 2
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Reconcile exact runtime seam admission bindings second
  infisical_cristexhub_prod_runtime_guarded_k8s:
    state: present
    definition: "{{ item }}"
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
    wait: false
    wait_timeout: 60
  loop: "{{ cristexhub_prod_runtime_admission_bindings }}"
  loop_control:
    label: "{{ item.metadata.name }}"
  register: cristexhub_prod_runtime_admission_binding_apply
  no_log: true

- name: Wait for exact runtime VAP bindings to become effective
  kubernetes.core.k8s_info:
    api_version: admissionregistration.k8s.io/v1
    kind: ValidatingAdmissionPolicyBinding
    name: "{{ item.metadata.name }}"
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  loop: "{{ cristexhub_prod_runtime_admission_bindings }}"
  loop_control:
    label: "{{ item.metadata.name }}"
  register: cristexhub_prod_runtime_admission_binding_state
  until:
    - cristexhub_prod_runtime_admission_binding_state.resources | length == 1
    - cristexhub_prod_runtime_admission_binding_state.resources[0].spec.policyName == item.spec.policyName
    - cristexhub_prod_runtime_admission_binding_state.resources[0].spec.validationActions == item.spec.validationActions
    - (cristexhub_prod_runtime_admission_binding_state.resources[0].spec.matchResources | default({})) == (item.spec.matchResources | default({}))
    - cristexhub_prod_runtime_admission_binding_state.resources[0].metadata.generation is defined
  retries: 30
  delay: 2
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Recheck all six Infisical CR inventories before granting PROD RBAC
  kubernetes.core.k8s_info:
    api_version: "{{ item.api_version }}"
    kind: "{{ item.kind }}"
    namespace: cristexhub-prod
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  loop: "{{ cristexhub_prod_runtime_bootstrap_expected_cr_kinds }}"
  register: cristexhub_prod_runtime_cr_inventory_recheck
  changed_when: false
  no_log: true

- name: Recheck all PROD Secrets before granting PROD RBAC
  kubernetes.core.k8s_info:
    api_version: v1
    kind: Secret
    namespace: cristexhub-prod
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  register: cristexhub_prod_runtime_target_secret_inventory_recheck
  changed_when: false
  no_log: true

- name: Refuse Infisical CR or Secret UID/resourceVersion races
  ansible.builtin.assert:
    that:
      - item.resources | map(attribute='metadata.name') | list == cristexhub_prod_runtime_cr_inventory.results[index].resources | map(attribute='metadata.name') | list
      - item.resources | map(attribute='metadata.uid') | list == cristexhub_prod_runtime_cr_inventory.results[index].resources | map(attribute='metadata.uid') | list
      - item.resources | map(attribute='metadata.resourceVersion') | list == cristexhub_prod_runtime_cr_inventory.results[index].resources | map(attribute='metadata.resourceVersion') | list
    fail_msg: Refusing a concurrent Infisical CR create, replace, or update during runtime seam admission staging.
  loop: "{{ cristexhub_prod_runtime_cr_inventory_recheck.results }}"
  loop_control:
    index_var: index
    label: "{{ item.item.kind }}"
  changed_when: false
  no_log: true

- name: Refuse PROD Secret UID/resourceVersion races
  ansible.builtin.assert:
    that:
      - item.resources | map(attribute='metadata.name') | list == cristexhub_prod_runtime_target_secret_inventory.resources | map(attribute='metadata.name') | list
      - item.resources | map(attribute='metadata.uid') | list == cristexhub_prod_runtime_target_secret_inventory.resources | map(attribute='metadata.uid') | list
      - item.resources | map(attribute='metadata.resourceVersion') | list == cristexhub_prod_runtime_target_secret_inventory.resources | map(attribute='metadata.resourceVersion') | list
    fail_msg: Refusing a concurrent PROD Secret create, replace, or update during runtime seam admission staging.
  loop:
    - "{{ cristexhub_prod_runtime_target_secret_inventory_recheck }}"
  loop_control:
    label: cristexhub-prod
  changed_when: false
  no_log: true

- name: Recheck exact runtime RBAC and source objects before granting PROD RBAC
  kubernetes.core.k8s_info:
    api_version: "{{ item.apiVersion }}"
    kind: "{{ item.kind }}"
    namespace: "{{ item.metadata.namespace | default(omit) }}"
    name: "{{ item.metadata.name }}"
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  loop: "{{ cristexhub_prod_runtime_rbac_objects + cristexhub_prod_runtime_source_objects }}"
  loop_control:
    label: "{{ item.kind }}/{{ item.metadata.name }}"
  register: cristexhub_prod_runtime_nonadmission_prestate_recheck
  changed_when: false
  no_log: true

- name: Refuse runtime RBAC or source object UID resourceVersion races
  ansible.builtin.assert:
    that:
      - item.resources | length == cristexhub_prod_runtime_prestate.results[nonadmission_index + 8].resources | length
      - item.resources | map(attribute='metadata.uid') | list == cristexhub_prod_runtime_prestate.results[nonadmission_index + 8].resources | map(attribute='metadata.uid') | list
      - item.resources | map(attribute='metadata.resourceVersion') | list == cristexhub_prod_runtime_prestate.results[nonadmission_index + 8].resources | map(attribute='metadata.resourceVersion') | list
    fail_msg: Refusing a concurrent runtime RBAC or source object create, replace, or update.
  loop: "{{ cristexhub_prod_runtime_nonadmission_prestate_recheck.results }}"
  loop_control:
    index_var: nonadmission_index
    label: "{{ item.item.kind }}/{{ item.item.metadata.name }}"
  changed_when: false
  no_log: true

- name: Requery exact runtime admission policies immediately before PROD RBAC
  kubernetes.core.k8s_info:
    api_version: admissionregistration.k8s.io/v1
    kind: ValidatingAdmissionPolicy
    name: "{{ item.metadata.name }}"
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  loop: "{{ cristexhub_prod_runtime_admission_policies }}"
  loop_control:
    label: "{{ item.metadata.name }}"
  register: cristexhub_prod_runtime_admission_policy_recheck
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Requery exact runtime admission bindings immediately before PROD RBAC
  kubernetes.core.k8s_info:
    api_version: admissionregistration.k8s.io/v1
    kind: ValidatingAdmissionPolicyBinding
    name: "{{ item.metadata.name }}"
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  loop: "{{ cristexhub_prod_runtime_admission_bindings }}"
  loop_control:
    label: "{{ item.metadata.name }}"
  register: cristexhub_prod_runtime_admission_binding_recheck
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Require exact ready and effective runtime admission closure before PROD RBAC
  ansible.builtin.assert:
    that:
      - cristexhub_prod_runtime_admission_policy_recheck.results | length == 4
      - cristexhub_prod_runtime_admission_binding_recheck.results | length == 4
      - item.resources | length == 1
      - item.resources[0].spec.failurePolicy == item.item.spec.failurePolicy
      - item.resources[0].spec.matchConstraints.resourceRules == item.item.spec.matchConstraints.resourceRules
      - (item.resources[0].spec.matchConditions | default([])) == (item.item.spec.matchConditions | default([]))
      - item.resources[0].spec.validations == item.item.spec.validations
      - item.resources[0].metadata.uid == cristexhub_prod_runtime_admission_policy_state.results[admission_index].resources[0].metadata.uid
      - item.resources[0].status.observedGeneration == item.resources[0].metadata.generation
      - item.resources[0].status.typeChecking is defined
      - item.resources[0].status.typeChecking.expressionWarnings | default([]) | length == 0
    fail_msg: Refusing PROD RBAC while a runtime admission policy is missing, drifted, or has type-check warnings.
  loop: "{{ cristexhub_prod_runtime_admission_policy_recheck.results | default([]) }}"
  loop_control:
    index_var: admission_index
    label: "{{ item.item.metadata.name }}"
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Require exact runtime admission binding closure before PROD RBAC
  ansible.builtin.assert:
    that:
      - item.resources | length == 1
      - item.resources[0].spec.policyName == item.item.spec.policyName
      - item.resources[0].spec.validationActions == item.item.spec.validationActions
      - (item.resources[0].spec.matchResources | default({})) == (item.item.spec.matchResources | default({}))
      - item.resources[0].metadata.uid == cristexhub_prod_runtime_admission_binding_state.results[binding_index].resources[0].metadata.uid
    fail_msg: Refusing PROD RBAC while a runtime admission binding is missing or drifted.
  loop: "{{ cristexhub_prod_runtime_admission_binding_recheck.results | default([]) }}"
  loop_control:
    index_var: binding_index
    label: "{{ item.item.metadata.name }}"
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Requery exact Operator PROD prerequisites immediately before writer RBAC
  kubernetes.core.k8s_info:
    api_version: "{{ item.api_version }}"
    kind: "{{ item.kind }}"
    namespace: "{{ item.namespace }}"
    name: "{{ item.name }}"
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  loop:
    - api_version: apps/v1
      kind: Deployment
      namespace: shared-services
      name: infisical-operator-controller
    - api_version: rbac.authorization.k8s.io/v1
      kind: Role
      namespace: cristexhub-prod
      name: infisical-operator-manager
    - api_version: rbac.authorization.k8s.io/v1
      kind: RoleBinding
      namespace: cristexhub-prod
      name: infisical-operator-manager
  register: cristexhub_prod_runtime_operator_prerequisites_recheck
  changed_when: false
  no_log: true

- name: Refuse Operator Deployment or PROD manager RBAC races before writer RBAC
  ansible.builtin.assert:
    that:
      - item.resources | length == 1
      - item.resources[0].metadata.uid == cristexhub_prod_runtime_operator_prerequisites.results[operator_index].resources[0].metadata.uid
      - >-
        operator_index == 0 and
        item.resources[0].metadata.generation == cristexhub_prod_runtime_operator_prerequisites.results[0].resources[0].metadata.generation or
        operator_index in [1, 2] and
        item.resources[0].metadata.resourceVersion == cristexhub_prod_runtime_operator_prerequisites.results[operator_index].resources[0].metadata.resourceVersion
    fail_msg: Refusing concurrent Operator Deployment or PROD manager RBAC drift before writer RBAC.
  loop: "{{ cristexhub_prod_runtime_operator_prerequisites_recheck.results }}"
  loop_control:
    index_var: operator_index
    label: "{{ item.item.kind }}/{{ item.item.name }}"
  changed_when: false
  no_log: true

- name: Reconcile exact runtime seam RBAC after admission and race checks
  infisical_cristexhub_prod_runtime_guarded_k8s:
    state: present
    definition: "{{ item }}"
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
    wait: false
    wait_timeout: 60
  loop: "{{ cristexhub_prod_runtime_rbac_objects }}"
  loop_control:
    label: "{{ item.kind }}/{{ item.metadata.name }}"
  register: cristexhub_prod_runtime_rbac_apply
  no_log: true

- name: Recheck Universal Auth credential after granting PROD RBAC
  kubernetes.core.k8s_info:
    api_version: v1
    kind: Secret
    namespace: cristexhub-prod
    name: cristexhub-prod-infisical-universal-auth
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  register: cristexhub_prod_runtime_secret_post_rbac_recheck
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Refuse Universal Auth credential race after PROD RBAC
  ansible.builtin.assert:
    that:
      - cristexhub_prod_runtime_secret_post_rbac_recheck.resources | length == 1
      - cristexhub_prod_runtime_secret_post_rbac_recheck.resources[0].metadata.uid == cristexhub_prod_runtime_credential.resources[0].metadata.uid
      - cristexhub_prod_runtime_secret_post_rbac_recheck.resources[0].metadata.resourceVersion == cristexhub_prod_runtime_credential.resources[0].metadata.resourceVersion
    fail_msg: Refusing a concurrent Universal Auth credential rotation or replacement after PROD RBAC.
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Recheck exact runtime source objects after granting PROD RBAC
  kubernetes.core.k8s_info:
    api_version: "{{ item.apiVersion }}"
    kind: "{{ item.kind }}"
    namespace: "{{ item.metadata.namespace }}"
    name: "{{ item.metadata.name }}"
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  loop: "{{ cristexhub_prod_runtime_source_objects }}"
  loop_control:
    label: "{{ item.kind }}/{{ item.metadata.name }}"
  register: cristexhub_prod_runtime_source_prestate_recheck
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Refuse runtime source UID resourceVersion races after PROD RBAC
  ansible.builtin.assert:
    that:
      - item.resources | length == cristexhub_prod_runtime_prestate.results[source_index + 10].resources | length
      - item.resources | map(attribute='metadata.uid') | list == cristexhub_prod_runtime_prestate.results[source_index + 10].resources | map(attribute='metadata.uid') | list
      - item.resources | length == 0 or item.resources[0].spec == item.item.spec
    fail_msg: Refusing a concurrent runtime source object create, replace, or update after PROD RBAC.
  loop: "{{ cristexhub_prod_runtime_source_prestate_recheck.results | default([]) }}"
  loop_control:
    index_var: source_index
    label: "{{ item.item.kind }}/{{ item.item.metadata.name }}"
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Reconcile canonical PROD Infisical Connection first
  infisical_cristexhub_prod_runtime_guarded_k8s:
    state: present
    definition: "{{ cristexhub_prod_runtime_source_objects[0] }}"
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
    wait: false
    wait_timeout: 60
  register: cristexhub_prod_runtime_source_connection_apply
  no_log: true

- name: Recheck Universal Auth credential immediately before Infisical Auth
  kubernetes.core.k8s_info:
    api_version: v1
    kind: Secret
    namespace: cristexhub-prod
    name: cristexhub-prod-infisical-universal-auth
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  register: cristexhub_prod_runtime_credential_before_auth
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Refuse Universal Auth credential race before Infisical Auth
  ansible.builtin.assert:
    that:
      - cristexhub_prod_runtime_credential_before_auth.resources | length == 1
      - cristexhub_prod_runtime_credential_before_auth.resources[0].metadata.uid == cristexhub_prod_runtime_credential.resources[0].metadata.uid
      - cristexhub_prod_runtime_credential_before_auth.resources[0].metadata.resourceVersion == cristexhub_prod_runtime_credential.resources[0].metadata.resourceVersion
    fail_msg: Refusing credential rotation immediately before PROD Infisical Auth reconciliation.
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Recheck canonical PROD Infisical Auth immediately before apply
  kubernetes.core.k8s_info:
    api_version: secrets.infisical.com/v1beta1
    kind: InfisicalAuth
    namespace: cristexhub-prod
    name: cristexhub-prod-infisical-auth
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  register: cristexhub_prod_runtime_source_auth_preapply_recheck
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Refuse PROD Infisical Auth UID or spec race immediately before apply
  ansible.builtin.assert:
    that:
      - cristexhub_prod_runtime_source_auth_preapply_recheck.resources | length == cristexhub_prod_runtime_prestate.results[11].resources | length
      - cristexhub_prod_runtime_source_auth_preapply_recheck.resources | map(attribute='metadata.uid') | list == cristexhub_prod_runtime_prestate.results[11].resources | map(attribute='metadata.uid') | list
      - cristexhub_prod_runtime_source_auth_preapply_recheck.resources | length == 0 or cristexhub_prod_runtime_source_auth_preapply_recheck.resources[0].spec == cristexhub_prod_runtime_source_objects[1].spec
    fail_msg: Refusing an Infisical Auth delete, recreate, or spec race immediately before apply.
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Reconcile canonical PROD Infisical Auth second
  infisical_cristexhub_prod_runtime_guarded_k8s:
    state: present
    definition: "{{ cristexhub_prod_runtime_source_objects[1] }}"
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
    wait: false
    wait_timeout: 60
  register: cristexhub_prod_runtime_source_auth_apply
  no_log: true

- name: Recheck Universal Auth credential immediately before StaticSecret
  kubernetes.core.k8s_info:
    api_version: v1
    kind: Secret
    namespace: cristexhub-prod
    name: cristexhub-prod-infisical-universal-auth
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  register: cristexhub_prod_runtime_credential_before_static_secret
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Refuse Universal Auth credential race before StaticSecret
  ansible.builtin.assert:
    that:
      - cristexhub_prod_runtime_credential_before_static_secret.resources | length == 1
      - cristexhub_prod_runtime_credential_before_static_secret.resources[0].metadata.uid == cristexhub_prod_runtime_credential.resources[0].metadata.uid
      - cristexhub_prod_runtime_credential_before_static_secret.resources[0].metadata.resourceVersion == cristexhub_prod_runtime_credential.resources[0].metadata.resourceVersion
    fail_msg: Refusing credential rotation immediately before PROD StaticSecret reconciliation.
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Recheck canonical PROD StaticSecret immediately before apply
  kubernetes.core.k8s_info:
    api_version: secrets.infisical.com/v1beta1
    kind: InfisicalStaticSecret
    namespace: cristexhub-prod
    name: cristexhub-prod-runtime
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  register: cristexhub_prod_runtime_source_static_secret_preapply_recheck
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Refuse PROD StaticSecret UID or spec race immediately before apply
  ansible.builtin.assert:
    that:
      - cristexhub_prod_runtime_source_static_secret_preapply_recheck.resources | length == cristexhub_prod_runtime_prestate.results[12].resources | length
      - cristexhub_prod_runtime_source_static_secret_preapply_recheck.resources | map(attribute='metadata.uid') | list == cristexhub_prod_runtime_prestate.results[12].resources | map(attribute='metadata.uid') | list
      - cristexhub_prod_runtime_source_static_secret_preapply_recheck.resources | length == 0 or cristexhub_prod_runtime_source_static_secret_preapply_recheck.resources[0].spec == cristexhub_prod_runtime_source_objects[2].spec
    fail_msg: Refusing a StaticSecret delete, recreate, or spec race immediately before apply.
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Reconcile canonical PROD Infisical StaticSecret last
  infisical_cristexhub_prod_runtime_guarded_k8s:
    state: present
    definition: "{{ cristexhub_prod_runtime_source_objects[2] }}"
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
    wait: false
    wait_timeout: 60
  register: cristexhub_prod_runtime_source_static_secret_apply
  no_log: true

- name: Wait for PROD Connection Auth and StaticSecret reconciliation readiness
  kubernetes.core.k8s_info:
    api_version: "{{ item.apiVersion }}"
    kind: "{{ item.kind }}"
    namespace: "{{ item.metadata.namespace }}"
    name: "{{ item.metadata.name }}"
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  loop: "{{ cristexhub_prod_runtime_source_objects }}"
  loop_control:
    label: "{{ item.kind }}/{{ item.metadata.name }}"
  register: cristexhub_prod_runtime_source_state
  until:
    - >-
      cristexhub_prod_runtime_source_state.resources | length == 1 and
      ((item.kind in ['InfisicalConnection', 'InfisicalAuth'] and
        (cristexhub_prod_runtime_source_state.resources[0].status.conditions |
         default([]) | selectattr('type', 'equalto', 'secrets.infisical.com/IsReady') |
         selectattr('status', 'equalto', 'True') |
         selectattr('observedGeneration', 'equalto', cristexhub_prod_runtime_source_state.resources[0].metadata.generation) |
         list | length == 1)) or
       (item.kind == 'InfisicalStaticSecret' and
        (cristexhub_prod_runtime_source_state.resources[0].status.conditions |
         default([]) | selectattr('type', 'equalto', 'secrets.infisical.com/LastReconcileStatus') |
         selectattr('status', 'equalto', 'True') |
         selectattr('observedGeneration', 'equalto', cristexhub_prod_runtime_source_state.resources[0].metadata.generation) |
         list | length == 1) and
        (cristexhub_prod_runtime_source_state.resources[0].status.conditions |
         default([]) | selectattr('type', 'equalto', 'secrets.infisical.com/LastSuccessfulReconcileAt') |
         selectattr('status', 'equalto', 'True') |
         selectattr('observedGeneration', 'equalto', cristexhub_prod_runtime_source_state.resources[0].metadata.generation) |
         list | length == 1)))
  retries: 30
  delay: 5
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Query generated PROD runtime target Secret post-state
  kubernetes.core.k8s_info:
    api_version: v1
    kind: Secret
    namespace: cristexhub-prod
    name: "{{ item.value.name }}"
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  loop: "{{ cristexhub_prod_runtime_bootstrap_target_contract | dict2items }}"
  loop_control:
    label: "{{ item.value.name }}"
  register: cristexhub_prod_runtime_target_secret_poststate
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Require exact generated PROD runtime target Secret post-state
  ansible.builtin.assert:
    that:
      - item.resources | length == 1
      - item.resources[0].metadata.name == item.item.value.name
      - item.resources[0].metadata.namespace == item.item.value.namespace
      - item.resources[0].type == item.item.value.type
      - (item.resources[0].data | default({})).keys() | list | sort == item.item.value.keys | sort
      - (item.resources[0].binaryData | default({})) | length == 0
      - not (item.resources[0].immutable | default(false) | bool)
      - (item.resources[0].metadata.ownerReferences | default([]) | length) == 0
      - (item.resources[0].metadata.finalizers | default([]) | length) == 0
      - (item.resources[0].metadata.labels | default({})) == cristexhub_prod_runtime_bootstrap_labels
      - (item.resources[0].metadata.annotations | default({})).keys() | list == ['secrets.infisical.com/version']
      - item.resources[0].metadata.annotations['secrets.infisical.com/version'] | length > 0
    fail_msg: Generated PROD target Secret is absent, drifted, or noncanonical.
  loop: "{{ cristexhub_prod_runtime_target_secret_poststate.results | default([]) }}"
  loop_control:
    label: "{{ item.item.value.name }}"
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Query exact runtime seam object post-state
  kubernetes.core.k8s_info:
    api_version: "{{ item.apiVersion }}"
    kind: "{{ item.kind }}"
    namespace: "{{ item.metadata.namespace | default(omit) }}"
    name: "{{ item.metadata.name }}"
    kubeconfig: "{{ cristexhub_prod_runtime_bootstrap_kubeconfig }}"
  loop: "{{ cristexhub_prod_runtime_manifests }}"
  loop_control:
    label: "{{ item.kind }}/{{ item.metadata.name }}"
  register: cristexhub_prod_runtime_poststate
  changed_when: false
  no_log: true
  when: not ansible_check_mode

- name: Require exact runtime seam post-state deep canonical equality
  ansible.builtin.assert:
    that:
      - item.resources | length == 1
      - item.resources[0].apiVersion == item.item.apiVersion
      - item.resources[0].kind == item.item.kind
      - item.resources[0].metadata.name == item.item.metadata.name
      - (item.resources[0].metadata.namespace | default('')) == (item.item.metadata.namespace | default(''))
      - (item.resources[0].metadata.ownerReferences | default([]) | length) == 0
      - (item.resources[0].metadata.finalizers | default([]) | length) == 0
      - >-
        (item.resources[0].metadata | dict2items |
        rejectattr('key', 'in', ['uid', 'resourceVersion', 'generation', 'creationTimestamp', 'managedFields', 'ownerReferences']) |
        list | items2dict) == item.item.metadata
      - >-
        (item.item.kind in ['InfisicalConnection', 'InfisicalAuth', 'InfisicalStaticSecret'] and
         item.resources[0].spec == item.item.spec) or
        (item.item.kind == 'Role' and item.resources[0].rules == item.item.rules) or
        (item.item.kind == 'RoleBinding' and item.resources[0].roleRef == item.item.roleRef and
         item.resources[0].subjects == item.item.subjects) or
        (item.item.kind == 'ValidatingAdmissionPolicy' and
         item.resources[0].spec.failurePolicy == item.item.spec.failurePolicy and
         item.resources[0].spec.matchConstraints.resourceRules == item.item.spec.matchConstraints.resourceRules and
         (item.resources[0].spec.matchConditions | default([])) == (item.item.spec.matchConditions | default([])) and
         item.resources[0].spec.validations == item.item.spec.validations) or
        (item.item.kind == 'ValidatingAdmissionPolicyBinding' and
         item.resources[0].spec.policyName == item.item.spec.policyName and
         item.resources[0].spec.validationActions == item.item.spec.validationActions and
         (item.resources[0].spec.matchResources | default({})) == (item.item.spec.matchResources | default({})))
    fail_msg: Runtime seam post-state is absent, drifted, or noncanonical after deep canonical comparison.
  loop: "{{ cristexhub_prod_runtime_poststate.results | default([]) }}"
  loop_control:
    label: "{{ item.item.kind }}/{{ item.item.metadata.name }}"
  changed_when: false
  no_log: true
  when: not ansible_check_mode

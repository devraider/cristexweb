---
- name: Transfer only the pending encrypted Infisical proxy recovery bundle
  hosts: k3s_servers
  gather_facts: true
  become: false
  any_errors_fatal: true
  serial: 1

  roles:
    - role: rclone_proxy_transfer

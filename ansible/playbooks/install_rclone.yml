---
- name: Install or roll back only the approved host rclone selector
  hosts: k3s_servers
  gather_facts: true
  become: true
  any_errors_fatal: true
  serial: 1

  roles:
    - role: rclone_install

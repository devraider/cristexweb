---
- name: Configure the guarded Keycloak PostgreSQL backup scheduler
  hosts: crtxweb
  gather_facts: false
  become: true
  any_errors_fatal: true
  serial: 1

  vars:
    postgresql_keycloak_backup_approved: false
    postgresql_keycloak_backup_mode: install
    postgresql_keycloak_backup_age_version: 1.2.1-1+b5
    postgresql_keycloak_backup_recipient_sha256: ce615905ec27fa6e085578d96e48dc798677b3f2ade5c417770f22e78248ad28
    postgresql_keycloak_backup_repository_root: "{{ playbook_dir | ansible.builtin.dirname | ansible.builtin.dirname }}"
    postgresql_keycloak_backup_source_closure_sha256: c05afb66756249152de4a5a289adc643b3a484db0fbc03eb6ed90606ba67dbe7

  pre_tasks:
    - name: Require the canonical backup scheduler contract
      ansible.builtin.assert:
        that:
          - postgresql_keycloak_backup_approved | bool
          - lookup('ansible.builtin.env', 'CRISTEXWEB_POSTGRESQL_KEYCLOAK_BACKUP_ENTRYPOINT') == 'v1'
          - (ansible_limit | default('') | string) == 'crtxweb'
          - (ansible_play_hosts_all | length) == 1
          - inventory_hostname == 'crtxweb'
          - postgresql_keycloak_backup_mode in ['install', 'test', 'restore', 'enable']
          - postgresql_keycloak_backup_age_version == '1.2.1-1+b5'
        fail_msg: Refusing backup scheduler execution outside the canonical bounded wrapper.
      changed_when: false

    - name: Collect installed package facts
      ansible.builtin.package_facts:
        manager: apt

    - name: Verify the pinned age package
      ansible.builtin.assert:
        that:
          - "'age' in ansible_facts.packages"
          - (ansible_facts.packages.age | length) == 1
          - ansible_facts.packages.age[0].version == postgresql_keycloak_backup_age_version
          - ansible_facts.packages.age[0].arch == 'amd64'
        fail_msg: Refusing an absent or drifted age package.
      changed_when: false

    - name: Inspect protected host backup inputs
      ansible.builtin.stat:
        path: "{{ item }}"
        follow: false
        checksum_algorithm: sha256
      loop:
        - /usr/local/bin/rclone
        - /opt/rclone/1.71.1/rclone
        - /home/paul/.config/rclone/rclone.conf
        - /home/paul/.config/cristexweb/backup/shared-database-backup.recipient
        - /etc/rancher/k3s/k3s.yaml
      register: postgresql_keycloak_backup_input_states

    - name: Verify protected host backup input metadata
      ansible.builtin.assert:
        that:
          - postgresql_keycloak_backup_input_states.results[0].stat.islnk | default(false)
          - postgresql_keycloak_backup_input_states.results[0].stat.lnk_source == '/opt/rclone/1.71.1/rclone'
          - postgresql_keycloak_backup_input_states.results[0].stat.pw_name == 'root'
          - postgresql_keycloak_backup_input_states.results[1].stat.isreg | default(false)
          - not (postgresql_keycloak_backup_input_states.results[1].stat.islnk | default(false))
          - postgresql_keycloak_backup_input_states.results[1].stat.pw_name == 'root'
          - postgresql_keycloak_backup_input_states.results[1].stat.mode == '0755'
          - postgresql_keycloak_backup_input_states.results[2].stat.isreg | default(false)
          - not (postgresql_keycloak_backup_input_states.results[2].stat.islnk | default(false))
          - postgresql_keycloak_backup_input_states.results[2].stat.pw_name == 'paul'
          - postgresql_keycloak_backup_input_states.results[2].stat.mode == '0600'
          - postgresql_keycloak_backup_input_states.results[3].stat.isreg | default(false)
          - not (postgresql_keycloak_backup_input_states.results[3].stat.islnk | default(false))
          - postgresql_keycloak_backup_input_states.results[3].stat.pw_name == 'paul'
          - postgresql_keycloak_backup_input_states.results[3].stat.mode == '0644'
          - postgresql_keycloak_backup_input_states.results[3].stat.checksum == postgresql_keycloak_backup_recipient_sha256
          - postgresql_keycloak_backup_input_states.results[4].stat.isreg | default(false)
          - postgresql_keycloak_backup_input_states.results[4].stat.gr_name == 'k3s-admin'
          - postgresql_keycloak_backup_input_states.results[4].stat.mode == '0640'
        fail_msg: Refusing unsafe rclone, recipient, or kubeconfig metadata.
      changed_when: false

    - name: Verify the sole configured rclone remote without reading its token
      ansible.builtin.command:
        argv:
          - /usr/local/bin/rclone
          - --config
          - /home/paul/.config/rclone/rclone.conf
          - listremotes
      become: false
      register: postgresql_keycloak_backup_rclone_remotes
      changed_when: false
      check_mode: false
      no_log: true

    - name: Require the exact Google Drive remote
      ansible.builtin.assert:
        that:
          - postgresql_keycloak_backup_rclone_remotes.stdout_lines == ['drive:']
        fail_msg: Refusing a host rclone configuration outside the exact drive remote.
      changed_when: false

  tasks:
    - name: Create protected backup directories
      ansible.builtin.file:
        path: "{{ item.path }}"
        state: directory
        owner: "{{ item.owner }}"
        group: "{{ item.group }}"
        mode: "{{ item.mode }}"
      loop:
        - {path: /etc/cristexweb-backup, owner: root, group: paul, mode: '0750'}
        - {path: /usr/local/libexec/cristexweb, owner: root, group: root, mode: '0755'}
        - {path: /var/lib/cristexweb-backup, owner: paul, group: paul, mode: '0700'}
        - {path: /var/lib/cristexweb-backup/postgresql, owner: paul, group: paul, mode: '0700'}
        - {path: /var/lib/cristexweb-backup/postgresql/keycloak, owner: paul, group: paul, mode: '0700'}

    - name: Install the public age recipient
      ansible.builtin.copy:
        src: /home/paul/.config/cristexweb/backup/shared-database-backup.recipient
        dest: /etc/cristexweb-backup/age.recipient
        owner: root
        group: paul
        mode: '0640'

    - name: Install the guarded backup executable
      ansible.builtin.copy:
        src: "{{ postgresql_keycloak_backup_repository_root }}/ansible/files/backup/postgresql-keycloak-backup"
        dest: /usr/local/libexec/cristexweb/postgresql-keycloak-backup
        owner: root
        group: root
        mode: '0755'

    - name: Install the isolated restore rehearsal executable
      ansible.builtin.copy:
        src: "{{ postgresql_keycloak_backup_repository_root }}/ansible/files/backup/restore-postgresql-keycloak-rehearsal"
        dest: /usr/local/libexec/cristexweb/restore-postgresql-keycloak-rehearsal
        owner: root
        group: root
        mode: '0755'

    - name: Install the systemd service unit
      ansible.builtin.copy:
        src: "{{ postgresql_keycloak_backup_repository_root }}/ansible/files/backup/cristexweb-postgresql-keycloak-backup.service"
        dest: /etc/systemd/system/cristexweb-postgresql-keycloak-backup.service
        owner: root
        group: root
        mode: '0644'
      register: postgresql_keycloak_backup_service_unit

    - name: Install the systemd timer unit
      ansible.builtin.copy:
        src: "{{ postgresql_keycloak_backup_repository_root }}/ansible/files/backup/cristexweb-postgresql-keycloak-backup.timer"
        dest: /etc/systemd/system/cristexweb-postgresql-keycloak-backup.timer
        owner: root
        group: root
        mode: '0644'
      register: postgresql_keycloak_backup_timer_unit

    - name: Reload systemd for installed units
      ansible.builtin.systemd_service:
        daemon_reload: true
      when:
        - postgresql_keycloak_backup_service_unit.changed or postgresql_keycloak_backup_timer_unit.changed

    - name: Keep the timer disabled until restore acceptance
      ansible.builtin.systemd_service:
        name: cristexweb-postgresql-keycloak-backup.timer
        enabled: false
        state: stopped
      when: postgresql_keycloak_backup_mode in ['install', 'test', 'restore']

    - name: Execute the separately approved one-time backup test
      ansible.builtin.systemd_service:
        name: cristexweb-postgresql-keycloak-backup.service
        state: started
      when:
        - postgresql_keycloak_backup_mode == 'test'
        - not ansible_check_mode

    - name: Execute the separately approved isolated restore rehearsal
      ansible.builtin.command:
        argv:
          - /usr/bin/timeout
          - --preserve-status
          - 3600
          - /usr/local/libexec/cristexweb/restore-postgresql-keycloak-rehearsal
      become: false
      register: postgresql_keycloak_backup_restore_result
      changed_when: true
      when:
        - postgresql_keycloak_backup_mode == 'restore'
        - not ansible_check_mode

    - name: Require the sanitized successful restore receipt
      ansible.builtin.assert:
        that:
          - >-
            postgresql_keycloak_backup_restore_result.stdout is match(
            '^restore_status=success schema=1 source_timestamp=20[0-9]{6}T[0-9]{6}Z '
            ~ 'source_closure_sha256=' ~ postgresql_keycloak_backup_source_closure_sha256 ~ ' '
            ~ 'target=isolated-emptydir catalog_table_count=[0-9]+ '
            ~ 'checksum=verified private_residue=none$')
        fail_msg: The isolated restore rehearsal did not produce the exact sanitized success receipt.
      changed_when: false
      when:
        - postgresql_keycloak_backup_mode == 'restore'
        - not ansible_check_mode

    - name: Inspect persisted fresh backup and restore acceptance evidence
      ansible.builtin.stat:
        path: "{{ item }}"
        follow: false
      loop:
        - /var/lib/cristexweb-backup/acceptance/postgresql/keycloak/backup.receipt
        - /var/lib/cristexweb-backup/acceptance/postgresql/keycloak/restore.receipt
      register: postgresql_keycloak_backup_acceptance_states
      when:
        - postgresql_keycloak_backup_mode == 'enable'
      changed_when: false
      no_log: true

    - name: Capture acceptance freshness reference epoch
      ansible.builtin.command:
        argv: [/usr/bin/date, +%s]
      register: postgresql_keycloak_backup_acceptance_now
      check_mode: false
      changed_when: false
      when:
        - postgresql_keycloak_backup_mode == 'enable'
      no_log: true

    - name: Require persisted fresh matching backup and restore acceptance evidence
      ansible.builtin.assert:
        that:
          - item.stat.isreg | default(false)
          - not (item.stat.islnk | default(false))
          - item.stat.pw_name == 'root'
          - item.stat.mode == '0600'
          - item.stat.mtime is defined
          - item.stat.mtime | int > (postgresql_keycloak_backup_acceptance_now.stdout | int - 86400)
      loop: "{{ postgresql_keycloak_backup_acceptance_states.results | default([]) }}"
      when:
        - postgresql_keycloak_backup_mode == 'enable'
      changed_when: false
      no_log: true

    - name: Read persisted acceptance receipts
      ansible.builtin.slurp:
        src: "{{ item }}"
      loop:
        - /var/lib/cristexweb-backup/acceptance/postgresql/keycloak/backup.receipt
        - /var/lib/cristexweb-backup/acceptance/postgresql/keycloak/restore.receipt
      register: postgresql_keycloak_backup_acceptance_contents
      when:
        - postgresql_keycloak_backup_mode == 'enable'
      changed_when: false
      no_log: true

    - name: Parse exact persisted acceptance identities without output
      ansible.builtin.set_fact:
        postgresql_keycloak_backup_acceptance_backup_receipt: "{{ postgresql_keycloak_backup_acceptance_contents.results[0].content | b64decode }}"
        postgresql_keycloak_backup_acceptance_restore_receipt: "{{ postgresql_keycloak_backup_acceptance_contents.results[1].content | b64decode }}"
        postgresql_keycloak_backup_acceptance_backup_timestamp: >-
          {{ postgresql_keycloak_backup_acceptance_contents.results[0].content | b64decode |
          regex_findall('timestamp=(20[0-9]{6}T[0-9]{6}Z)') | first | default('') }}
        postgresql_keycloak_backup_acceptance_restore_source_timestamp: >-
          {{ postgresql_keycloak_backup_acceptance_contents.results[1].content | b64decode |
          regex_findall('source_timestamp=(20[0-9]{6}T[0-9]{6}Z)') | first | default('') }}
        postgresql_keycloak_backup_acceptance_backup_schema: >-
          {{ postgresql_keycloak_backup_acceptance_contents.results[0].content | b64decode |
          regex_findall('schema=([0-9]+)') | first | default('') }}
        postgresql_keycloak_backup_acceptance_restore_schema: >-
          {{ postgresql_keycloak_backup_acceptance_contents.results[1].content | b64decode |
          regex_findall('schema=([0-9]+)') | first | default('') }}
        postgresql_keycloak_backup_acceptance_backup_closure: >-
          {{ postgresql_keycloak_backup_acceptance_contents.results[0].content | b64decode |
          regex_findall('source_closure_sha256=([0-9a-f]{64})') | first | default('') }}
        postgresql_keycloak_backup_acceptance_restore_closure: >-
          {{ postgresql_keycloak_backup_acceptance_contents.results[1].content | b64decode |
          regex_findall('source_closure_sha256=([0-9a-f]{64})') | first | default('') }}
      when:
        - postgresql_keycloak_backup_mode == 'enable'
      changed_when: false
      no_log: true

    - name: Require exact current-source-bound acceptance receipts
      ansible.builtin.assert:
        that:
          - >-
            postgresql_keycloak_backup_acceptance_backup_receipt is match(
            '^backup_status=success schema=1 service=postgresql database=keycloak '
            ~ 'timestamp=20[0-9]{6}T[0-9]{6}Z archive_bytes=[1-9][0-9]* '
            ~ 'duration_seconds=[0-9]+ readback=verified private_residue=none '
            ~ 'source_closure_sha256=' ~ postgresql_keycloak_backup_source_closure_sha256 ~ '$')
          - >-
            postgresql_keycloak_backup_acceptance_restore_receipt is match(
            '^restore_status=success schema=1 source_timestamp=20[0-9]{6}T[0-9]{6}Z '
            ~ 'source_closure_sha256=' ~ postgresql_keycloak_backup_source_closure_sha256 ~ ' '
            ~ 'target=isolated-emptydir catalog_table_count=[0-9]+ '
            ~ 'checksum=verified private_residue=none$')
          - postgresql_keycloak_backup_acceptance_backup_timestamp | length == 16
          - postgresql_keycloak_backup_acceptance_restore_source_timestamp | length == 16
          - postgresql_keycloak_backup_acceptance_backup_timestamp == postgresql_keycloak_backup_acceptance_restore_source_timestamp
          - postgresql_keycloak_backup_acceptance_backup_schema == '1'
          - postgresql_keycloak_backup_acceptance_restore_schema == '1'
          - postgresql_keycloak_backup_acceptance_backup_closure == postgresql_keycloak_backup_source_closure_sha256
          - postgresql_keycloak_backup_acceptance_restore_closure == postgresql_keycloak_backup_source_closure_sha256
      when:
        - postgresql_keycloak_backup_mode == 'enable'
      changed_when: false
      no_log: true

    - name: Enable and validate the accepted timer with rollback
      block:
        - name: Enable the accepted daily timer
          ansible.builtin.systemd_service:
            name: cristexweb-postgresql-keycloak-backup.timer
            enabled: true
            state: started
          when: postgresql_keycloak_backup_mode == 'enable'
        - name: Verify timer active after enable
          ansible.builtin.command:
            argv: [/usr/bin/systemctl, is-active, cristexweb-postgresql-keycloak-backup.timer]
          register: postgresql_keycloak_backup_enable_active
          changed_when: false
          check_mode: false
          when:
            - postgresql_keycloak_backup_mode == 'enable'
            - not ansible_check_mode
        - name: Verify timer enabled after enable
          ansible.builtin.command:
            argv: [/usr/bin/systemctl, is-enabled, cristexweb-postgresql-keycloak-backup.timer]
          register: postgresql_keycloak_backup_enable_enabled
          changed_when: false
          check_mode: false
          when:
            - postgresql_keycloak_backup_mode == 'enable'
            - not ansible_check_mode
        - name: Require timer health after enable
          ansible.builtin.assert:
            that:
              - postgresql_keycloak_backup_enable_active.rc == 0
              - postgresql_keycloak_backup_enable_active.stdout == 'active'
              - postgresql_keycloak_backup_enable_enabled.rc == 0
              - postgresql_keycloak_backup_enable_enabled.stdout == 'enabled'
            fail_msg: Timer failed post-enable validation.
          when:
            - postgresql_keycloak_backup_mode == 'enable'
            - not ansible_check_mode
      rescue:
        - name: Roll back timer after failed post-enable validation
          ansible.builtin.systemd_service:
            name: cristexweb-postgresql-keycloak-backup.timer
            enabled: false
            state: stopped
          when:
            - postgresql_keycloak_backup_mode == 'enable'
            - not ansible_check_mode
        - name: Refuse timer acceptance after rollback
          ansible.builtin.fail:
            msg: Timer enablement rolled back after failed validation.

  post_tasks:
    - name: Collect final service facts
      ansible.builtin.service_facts:

    - name: Inspect final timer active state
      ansible.builtin.command:
        argv:
          - /usr/bin/systemctl
          - is-active
          - cristexweb-postgresql-keycloak-backup.timer
      become: false
      register: postgresql_keycloak_backup_timer_active
      changed_when: false
      check_mode: false
      failed_when: false
      when:
        - postgresql_keycloak_backup_mode == 'enable'
        - not ansible_check_mode

    - name: Inspect final timer enabled state
      ansible.builtin.command:
        argv:
          - /usr/bin/systemctl
          - is-enabled
          - cristexweb-postgresql-keycloak-backup.timer
      become: false
      register: postgresql_keycloak_backup_timer_enabled
      changed_when: false
      check_mode: false
      failed_when: false
      when:
        - postgresql_keycloak_backup_mode == 'enable'
        - not ansible_check_mode

    - name: Verify critical services and requested timer state
      ansible.builtin.assert:
        that:
          - ansible_facts.services['k3s.service'].state == 'running'
          - ansible_facts.services['tailscaled.service'].state == 'running'
          - >-
            ansible_check_mode or postgresql_keycloak_backup_mode != 'enable' or
            (postgresql_keycloak_backup_timer_active.rc == 0 and
             postgresql_keycloak_backup_timer_active.stdout == 'active' and
             postgresql_keycloak_backup_timer_enabled.rc == 0 and
             postgresql_keycloak_backup_timer_enabled.stdout == 'enabled')
        fail_msg: Refusing acceptance because service health or timer state is wrong.
      changed_when: false
